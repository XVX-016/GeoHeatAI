# Vercel deployment notes

- This app is configured as a TanStack Start/Vite frontend.
- Build command: npm run build
- Output directory: dist
- Node.js version: 20+

For Vercel, connect the repository and set the project root to the repository root.
